"""MySQL connection stub — not used in UI-only demo.

When ready to connect MySQL, implement connect() and use mysql-connector-python.
"""

# Future: import mysql.connector


def connect():
    """Return a MySQL connection. Not implemented in UI demo."""
    raise NotImplementedError(
        "MySQL is not configured yet. The app runs with in-memory mock data."
    )
